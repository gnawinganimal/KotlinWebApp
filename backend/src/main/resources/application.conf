ktor {
    deployment {
        port = 9090
        environment = development
        autoreload = true
        watch = [ KotlinWebapp ]
    }

    application {
        id = KotlinWebapp
        modules = [ com.jamdan2.kotlinwebapp.MainKt.main ]
    }
}
